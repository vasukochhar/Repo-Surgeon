.pytest_cache/
__pycache__/
*.py[cod]
*.egg-info/

# Local-only fixture used for the live Codex smoke test.
fixtures/live_codex_smoke/
.venv/
.env

# Per-job data-flow dumps from E2E test runs. May contain repo source and
# scanner output, and grows unboundedly — keep it local.
test_results/
